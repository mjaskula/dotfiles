Provisioning:

Goals:
- limit shell script as much as possible (do everything with ansible)
- remove all provisioning from the dotfiles scripts


Manual steps:
---------------
  xcode-select --install
  git clone https://github.com/mjaskula/dotfiles.git .dotfiles
  <edit provision.yml for machine type>
  ./.dotfiles/provisioning/bootstrap.sh

TODO
--------

.dotfiles
  - replace bin/dot with ansible
    - OSX config
    - APP config


locate db:
  sudo launchctl load -w /System/Library/LaunchDaemons/com.apple.locate.plist



VM
----------------
EL Capitan VM:
  http://www.insanelymac.com/forum/topic/309654-run-vanilla-os-x-el-capitan-yosemite-or-mavericks-in-virtualbox-5010-on-a-windows-host/